# --- Proveedor por defecto ---
LLM_PROVIDER=replicate

# (Opcional) Fallback si falla Replicate: 'oss' | 'openai' | 'anthropic'
LLM_FALLBACK_PROVIDER=oss

# --- Replicate ---

REPLICATE_MODEL=openai/gpt-oss-20b
# REPLICATE_VERSION=            # solo si tu código usa /v1/predictions con "version"

# --- Mantén solo si usas fallback correspondiente ---
# OSS_API_BASE=http://llm:11434
# OSS_MODEL=gpt-oss-20b
# OPENAI_API_KEY=...
# OPENAI_MODEL=gpt-4o-mini
# ANTHROPIC_API_KEY=...
# ANTHROPIC_MODEL=claude-3-5-sonnet-latest

# --- Resto de tu app (deja igual) ---
OPENAI_TRANSCRIBE_MODEL=whisper-1
MAX_MEDIA_BYTES=104857600
FLASHCARD_TARGET=12
QUIZ_TARGET=14
DEEP_TARGET=8
CLOZE_TARGET=10